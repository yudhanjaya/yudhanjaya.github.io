---
layout: none
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }} — Posts</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ site.url }}/feed.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url }}/" rel="alternate" type="text/html"/>
  <id>{{ site.url }}/</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author>
    <name>{{ site.title | xml_escape }}</name>
  </author>

  {%- assign posts = site.pages | where: "parent", "Posts" | sort: "date" | reverse -%}
  {%- for post in posts -%}
  {%- if post.date -%}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.permalink }}" rel="alternate" type="text/html"/>
    <id>{{ site.url }}{{ post.permalink }}</id>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    {%- if post.excerpt %}
    <summary>{{ post.excerpt | xml_escape }}</summary>
    {%- endif %}
  </entry>
  {%- endif -%}
  {%- endfor %}
</feed>
